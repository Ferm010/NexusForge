package com.example.nexusforge.backend

/**
 * Замените значение на OAuth 2.0 Web Client ID из вашего Firebase проекта.
 * Найти его можно в Firebase Console → Authentication → Sign-in method → Google → Web SDK configuration,
 * или в файле google-services.json в поле oauth_client[].client_id (client_type = 3).
 */
const val WEB_CLIENT_ID = "REPLACE_WITH_YOUR_WEB_CLIENT_ID.apps.googleusercontent.com"
